# 2026-09-07 工作日志

- 09:26 **v1.0.2 六项更新完成（未推 GitHub，本地试玩中）**。① 转移伏击：`advanceBranch(state, rng)` 在分支未搜满（<3 次）时深入有概率被残余敌人纠缠（0.55×(1-已搜/3)，搜 0 次约 55%、搜满 100% 安全），伏击写入 `state.encounter` 走四选一抉择且不消耗转移时间；从 `rollEncounter` 抽出 `pickEnemy` 复用。② Boss 掉落：`rollGearDrop(rng, danger, luckBias, minTier)` 新增 minTier 保底；霸主击杀掉落 danger+3/0.8/保底≥max(2,danger)，霸主尸体（corpse.boss）搜刮必掉 danger+2 遗物并 plog。③ 掉落槽位：rollGearDrop 全 6 槽加权（武器24/护甲22/副武器/头部14/腿部13/饰品13，原只掉 3 槽）；伤害类投掷物：开局 UI 读取快捷·投掷槽（无 extractBonus 且有库存）写入 `run.quickThrow`，fight 中 45% 概率先手引爆（20% 敌方最大生命，敌方 Unit 先 build 再 takeDamage）；增益物品：快捷·增益槽药品出击中可「🧪 使用」（applyBuff 扣基地 medicines，run.buffCharges+1），fight 消耗 1 次 → effAttrs 力量+5/敏捷+5/耐力+3/意志+2 参战。④ 词条体系重做：GEAR_AFFIXES（affixes.ts）与 economy.ts 制造词缀池全部改为特殊属性（生命值/暴击率/搜刮运势/经验获取/金币获取），不再加六维；装备灰字=槽位基础属性 ×(1+0.2×tier) 成长；词条数量 白 6%/绿 15%/蓝紫 45%/黄+ 75% 出第 2 条，再 50% 出第 3 条；GearCombatBonus/AppliedAffix/CombatBonus 加 xpBonus/coinBonus，aggregateGearCombat/buildSortieLoadout 聚合，persistRunResult 结算经验×(1+xpBonus)、金币按折算价值×coinBonus 追加；制造 rollGear 改用 scaleGearCombat。⑤ 末世行止暗色化：MenuDrawer.tsx + views.tsx 全量 stone/white → zinc 暗色映射（Python 批量替换 98 处），与避难所一致。⑥ 游戏名：src 全量 全民求生→全境求生（保留 docs/reskin-design- 文件名引用）；ViewMarket 重写：×1/×5 选择 → 琥珀确认条（显示总价、币不足提示）→ 确认购买 doBuy 调 buyMedicine(id, qty)（引擎原生支持 qty 与币校验）+ 购买成功反馈。验证：tsc 0 错、lint 仅余 hono.ts×2、build:client 过、冒烟 11 断言全过（300 掉落全 6 槽、词条全特殊属性、Boss 保底品阶、搜满 3 次转移 0 伏击/未搜 30 次 19 伏击、增益/手雷日志、霸主遗物）。坑：Vite 启动需清 node_modules/.vite 缓存，沙箱 safe-delete 拦截 vite 自删 → 手动 shutil.rmtree。dev server 跑在 http://localhost:5173（后台任务 uEW7Va，Redis 报错为上游 API 无本地 Redis，不影响纯前端玩法）。**未 push，待用户验收后按 1.0.2 发版（记得开 VPN）**。

## v1.0.2 发布（2026-09-07 10:07）
- package.json 版本号 1.0.1 → 1.0.2，提交 a197e98，tag v1.0.2 已推送至 origin/main（VPN 开启，推送一次成功）。
- 提交内容：转移遇敌、boss 高阶掉落、全部位装备掉落+投掷物/增益、暗色主题+词条系统重做、游戏名统一为"全境求生·系统搜打撤"、市场确认+批量购买。

## v1.0.3 本地更新完成（2026-09-07 晚，未 push，待用户验收）
- 用户需求：骨折/失血/震伤三类战后 debuff 减基础六维 1/3（红显），血条三阶段（≤30%红/≤70%橙/>70%绿/0灰），debuff 随血量降低概率上升；升级满血且清 debuff；出击实时显示并影响角色、恢复用品可消 debuff；临时背包装备可佩戴/替换（换装影响后续出击）；出击页增加六维/增益/Debuff 显示；出击概率弹药补给（不占背包直补弹匣）；深化六维属性数值体系。
- 实现要点：
  - recovery.ts：`INJURY_ATTR_PENALTY`（骨折→力量/敏捷/耐力×1/3；失血→体质/耐力×1/3；震伤→感知/意志×1/3；感染→体质/意志×1/4；疲惫→全×1/4），`applyInjuryToBase`（单属性下限1、总减上限0.6），`hpStage`/`HP_STAGE_META` 三阶段着色，`rollCombatInjuries(rng,hpPct,attrs)`：震伤全阶段 P= (0.12+0.38×(1-p))×意志抗；失血仅<70% P=0.45×((0.7-p)/0.7)×体质抗；骨折仅<30% P=0.6×((0.3-p)/0.3)。意志抗=max(0.55,1-意志×0.015)、体质抗=max(0.6,1-体质×0.01)。
  - ExtractionEngine.ts：`runEffectiveAttributes`（基础→debuff 削基→+装备加成→+固定加成）、`deriveAttrEffects`（背包容量=基础+⌊力量/5⌋；timeScale=clamp(1-(速度-10)×0.02,0.72,1.12)；潜行=速度差×0.03；续航分钟=耐力×2；避敌=(感知-10)×0.02；额外搜刮=min(0.6,感知×0.02)）、`equipCarriedGear/unequipRunGear/cureInjuries/checkFatigue`、`grantLoot/search` 弹药补给 AMMO_CACHE_CHANCE=0.3 直补弹匣；`createRun` 初始化 injuries/equipped/baseAttributes/attrBonusFixed/bonusBase。
  - state.ts：`grantSortieXp` 升级时 currentHp 补满 + injuries=[] 清 debuff。
  - play/route.tsx：`attrBars` 削减值红显(-diff)；资料卡/出击状态条均用 hpStage 三阶段色；出击显示六维(attrBars 对比基础)、buff(run.buffCharges)、debuff 芯片(injuryAttrTextOf)；新增本局穿戴面板(卸下按钮)、临时背包装备"🎽佩戴"；药品/医疗"💊使用"同时治疗+消 debuff；start 预佩戴身上装备进本局。
- 验证：tsc 0 错、lint 仅余 hono.ts×2（按约定不动）、build:client 过（3.61s）、冒烟 25 断言全过。package.json 1.0.2→1.0.3（本地，未 push）。dev server http://localhost:5173。
- **未 push**：用户要求先本地试玩验收，确认后再按 1.0.3 发版（开 VPN，git 7890 代理）。

## v1.0.3 补丁：四处 bug 修复（用户验收反馈）
- 反馈：①带伤出击未带入 debuff/削弱属性；②角色面板与出击血量对不上（驻防加成只加最大血、未加初始血）；③六维显示格式；④出击装备未显示加成值。
- 修复：
  - ① `start()` 调 createRun 时传入 `injuries: status?.injuries ?? []`，角色档案已有伤势（骨折/失血/震伤/疲惫）随出击带入本局，`runEffectiveAttributes` 据此削弱六维（`recovery.applyInjuryToBase` 削基础六维）。
  - ② HP 锚定：新增 `ExtractionRunState.startMaxHp/baseMaxHp`。`start()` 算 `garrisonHp = computeShelterBonuses(...).startHpBonus`（医疗站临时加成），`startMax = baseMax + garrisonHp`、`startHp = min(currentHp + headStart + garrisonHp, startMax)`，createRun 把 condition.hp.max/current 钉到 startMaxHp。`fight()` 建战斗单位后用 `survivorUnit.attributes.addModifier(FIXED MAX_HP, delta=startMaxHp-naturalMax)` 把单位 maxHp 也锚定到 startMaxHp，避免派生 maxHp 漂移、且只在本局生效。`persistRunResult` 回写用 `baseMaxHp`（剔除驻防加成，不泄漏进角色档案）；`finalHp` 封顶 baseMaxHp。出击状态条加「含临时驻防加成 +X 气血（仅本局生效）」提示。
  - ③ `attrBars` 改为 `最终值(+加成)(-减少)`：有减少时最终值粉红(pink-400)、减少值红(rose-500)、加成绿(emerald)；角色面板改用 `charEffectiveAttrs`（基础−伤势+装备）作最终值、`gearAttrBonus` 作加成、原始属性作基准，格式统一。
  - ④ 新增 `GearBonusChips`（属性+战斗词条 chip），用于出击「本局穿戴」面板与临时背包装备项，直观对比换装前后加成。
- 验证：tsc 0 错、lint 仅余 hono.ts×2（按约定不动）、build:client 过（3.67s）、引擎冒烟 8 断言全过（injuries 带入/startMaxHp 锚定/fracture 削力量 12→8/shellShock 削感知 10→6/fight 后 maxHp 仍=startMaxHp）。仍**未 push**，待用户本地试玩（http://localhost:5173）。
- 注意：本补丁把出击 maxHp 强制钉为「档案 maxHp + 临时驻防加成」，会抵消装备/词条自带的 hpBonus 对战斗血量上限的贡献（仅保留驻防临时部分）；若用户希望装备 hpBonus 也计入出击上限，需另行调整。

## v1.0.3 第二轮四处 bug 修复（用户最新反馈，本地未 push）
- 反馈：①分配自由能力值到「体质」，最大生命值未增加（此前约定体质与最大生命值挂钩）；②出击中角色切到背包页不能换装备/道具（提示"该角色出击中"），且出击页副本状态实时同步到角色页/背包页（含副本换装同步回装备栏），出击中不得在角色页/背包页恢复血量或更换装备道具；③六维显示重设：最终值（基础值+加成值-减损值），基础白、加成绿、减损红；④减少出击弹药获取数量，改为个位数~十几个随机。
- 修复：
  - ① `recovery.ts` 导出 `deriveMaxHp(attrs, hpBonus=0)=Math.round(400+vitality*20+endurance*3+hpBonus)`（原私有）；`state.ts` 新增 `recomputeMaxHpFor(state,id)`：按 `deriveMaxHp(p.attributes, aggregateTraitCombat(p.traits).hpBonus)` 重算 maxHp，若 newMax>当前则同步补 currentHp（加点不丢血）；`allocateFreePoint`/`chooseTraitPick` 末尾调用 → 体质加点即涨最大生命。
  - ④ `ExtractionEngine.ts`：`AMMO_CACHE_MIN=3`（个位数）/`AMMO_CACHE_MAX=15`（十几个），出击随机补给量由 8~20 改为 3~15。
  - ③ `play/route.tsx` `attrBars` 重写：显示 `最终值(基础值+加成值-减损值)`，base 白、bonus 绿(emerald)、reduction 红(rose)，有减损时最终值粉红(pink-400)；新增 `injuryReduction(base,injuries)`、`traitAttrBonus(s)` 辅助；JSX 用 ASCII 括号规避 TS1127。
  - ② 架构：把出击 `run` 状态从 SortiePanel 提升到 SurvivalHub（run/setRun/runRef/rngRef/writtenRef/syncRun），hooks 置于 `if(!user) return` 前以守 rules-of-hooks；CharacterPanel/InventoryPanel 收 `run` 参数。出击中：背包页顶部锁横幅"⚠ 该角色正在出击中，装备/道具更换已锁定…"、6 槽渲染 runEquippedMap、卸下/快捷槽/装备按钮禁用；角色页 `warbandHp(id)` 实时读 `run.condition.resources.hp`，伤势治疗锁为"该角色出击中，请在出击页使用道具消除"；出击页六维用 run.injuries，`persistRunResult` 把 `run.equipped` 回写 `state.equipped`（副本换装同步背包装备栏）。
- 验证：tsc 0 错、lint 仅余 hono.ts×2（按约定不动）、build:client 过、引擎冒烟 7 断言全过（体质加点 maxHp 上涨）。仍**未 push**，待用户本地试玩（http://localhost:5173）。

## v1.0.3 新需求①+②：实时经验/升级同步 + 副本换装仅改最大血量（本地未 push）
- 需求①：出击页也显示角色经验值并随打怪实时增长；出击途中升级→状态全满+清伤势+提示天赋三选一并即时生效；升级时的能力加点与天赋选择可在角色页或出击页任一边操作，两边同步。
- 需求②：出击时临时背包里带「增加气血」的装备，佩戴后仅加最大血量、脱下仅减最大血量、不影响当前血量（满血减最大则当前同减、满血加最大则当前不变）；「加体质点」最大+当前一起提升。
- 实现：
  - types.ts：`ExtractionRunState` 新增 `profileMaxHpBonus`（出击途中档案最大血量的相对增量）。
  - ExtractionEngine.ts：新增 `effectiveRunMaxHp(state)=startMaxHp + 装备气血和 + profileMaxHpBonus`、`recomputeRunMaxHp(state)`（max=有效上限，current=min(max,当前)）；`createRun` 初始化 max=起点+已穿戴装备气血；`equipCarriedGear/unequipRunGear` 调 `recomputeRunMaxHp`（换装仅改最大、current 夹取，实现需求②语义）；`fight()` 锚点改用 `effectiveRunMaxHp`（装备气血+途中加点/词条增量均即时生效，修复此前锚点把装备 hpBonus「抵消」的 bug）。
  - play/route.tsx（SurvivalHub）：新增 `onAllocatePoint/onPickTrait`（档案层 allocateFreePoint/chooseTraitPick + 同步 `run.condition.resources.hp` 的 max/current 与 `baseAttributes`/profileMaxHpBonus，使加点/词条在副本内即时生效、角色页与出击页共用即天然双向同步）；`stateRef`（置于 early-return 前守 rules-of-hooks）取最新状态避免连点丢更新。`doEncounter` 战斗后实时结算经验（`buildSortieLoadout(...).bonus.xpBonus` 折算）调 `grantSortieXp` 写入档案，途中升级则副本状态回复全满+清伤势。`persistRunResult` 改 `xpGained:0`（经验已实时入账，避免重复结算），仅保留拾荒嗅觉金币加成。CharacterPanel/SortiePanel 改用 `onAllocatePoint/onPickTrait`，SortiePanel 新增等级/经验条 + 加点 + 词条三选一 UI（读 `active` 共享档案，自动与角色页同步）。
- 验证：tsc 0 错、lint 仅余 hono.ts×2（按约定不动）、build:client 过（3.57s）、新增 `scripts/smoke_req.ts` 引擎冒烟 18 断言全过（换装仅改 max 且 current 按规则夹取、满血卸下 current 同减、非满不受影响；grantSortieXp 升级满血清伤势+三选一+连升；体质 +1 最大+当前同 +20；createRun +40 装备→副本 max 640）。**未 push**，待用户本地试玩（http://localhost:5173，开 VPN 后才可发 1.0.3）。

## v1.0.3 第三轮四处 bug 修复并发布（2026-09-07 晚）
- 用户验收反馈：①出击打怪经验没获取、撤离也不结算（要实时获取）；②出击途中换装备、撤离后背包页装备栏没同步（回到出击前佩戴）；③没穿戴装备仍有绿字加成（词条六维被当成绿字加成）；④减少出击弹药获取概率。要求直接发 1.0.3 并记录 1.0.2→1.0.3 全部改动、完善 README。
- 修复：
  - ① `doEncounter`：`xpBefore` 原在 `resolveEncounter` 之后读取 → `xpDelta` 恒为 0 → `grantSortieXp` 从未触发（经验既不在战斗中实时获取，而 `persistRunResult` 又已改 `xpGained:0` 故撤离也不结算）。修复：把 `xpBefore` 提前到 `resolveEncounter` 之前捕获，战斗后 `xpDelta>0` 即按 `buildSortieLoadout(...).bonus.xpBonus` 折算并实时 `grantSortieXp` 写入档案、途中升级则副本满血清伤势；出击页经验条读 `active` 共享档案实时刷新（UI 此前已就绪）。
  - ② `persistRunResult`：成功撤离时，副本内换上的装备若尚未入库（如副本掉落品直接穿戴），`ownedIds` 不含其 id → 写回被跳过 → 回到出击前佩戴。修复：装备写回前先把本局 `s.equipped` 中未入库的 gear 并入 `after.gear`（仅 `!failed` 成功撤离才入库；阵亡/超时跳过写回，由 `applyFailureGearLoss` 按概率夺装备），再写回 `state.equipped[prof.id]`，使副本换装（含掉落装备）同步保留。
  - ③ `play/route.tsx` 战团成员卡六维：因 `chooseTraitPick` 选取时已把词条六维修饰并入 `s.attributes`（基础六维），而 `traitAttrBonus(s)` 又额外叠加一次 → 重复计数且未穿戴装备仍显示绿字。修复：绿字仅显示 `gearAttrBonus(state, s.id)`，词条六维归入白色基础值；删除已无用的 `traitAttrBonus` 函数及未用的 `SurvivorProfile` 导入。
  - ④ `ExtractionEngine.ts`：`AMMO_CACHE_CHANCE` 0.3 → 0.15，削减出击过程中「搜刮翻出弹药箱」概率。
- 发布：package.json 版本 1.0.3（此前本地已置位）；提交 `f8b64dd`，tag `v1.0.3`，已 `git push origin main --tags` 至 GitHub（VPN/7890 代理开启，一次推送成功）。README 增补 v1.0.3 与各版本更新日志、成长/换装规则说明。scripts/smoke_req.ts 加 eslint-disable（仅 `any`），保持 lint 仅余 hono.ts×2。
- 验证：tsc 0 错、lint 仅余 hono.ts×2（按约定不动）、build:client 过（3.54s）、`scripts/smoke_req.ts` 19 断言全过。

## v1.0.4 本地修复（用户最新验收反馈，未 push）
- 反馈（先本地改、暂不推）：①出击打怪依然没获取经验（再查）；②骨折没对应药物清 debuff，市场应加「夹板绷带」50 废土币、恢复量同止血绷带且能清骨折，并能用 1 止血绷带+2 废金属制作；③出击临时背包加「临时制作台」可合成医疗制作台物品（消耗本局背包材料），并把废土币显示从「×1=25」改为「×25=25」。
- 修复：
  - ① **经验 bug 复现并修复**：上一轮把 `xpBefore` 提前的编辑实际未落盘（文件里仍是 `resolveEncounter` 之后读取 → `xpDelta` 恒 0）。本次重新把 `xpBefore` 提前到战斗前捕获，并用 `scripts/smoke_req3.ts` 以 `fight()` 直接驱动验证 `xpGained` 战后增长（0→23）。顺带改 `ExtractionEngine.ts` fight 日志「撤离成功后结算」→「已实时结算入角色档案」。
  - ② `state.ts`：`MedicineId` 增加 `splint`；`MEDICINES` 加「夹板绷带」（healPct 0.12/healFlat 20/treats 骨折/costCoins 50，市场因迭代 MEDICINES 自动出现）；`MED_CRAFT_RECIPES` 加 splint 配方（base：金属2+化工1+25币；`sortieNeeds`：绷带1+废金属2）；`LOOT_MEDICINE_MAP` 加 `splint`（临时背包可识别/使用清骨折）。`content.ts` `LOOT` 加 splint 战利品（并 `export const LOOT` 以便测试）。
  - ③ `play/route.tsx`：新增模块级 `MATERIAL_TO_LOOT`（金属→scrap/化工→chempack/食物→ration/电子→parts）、`MED_LOOT_TEMPLATE`（药品→loot 模板）、`LOOT_NAME`、`SORTIE_MED_CRAFT`（由 `MED_CRAFT_RECIPES` 派生，优先用 `sortieNeeds`，否则按材料大类映射）；SortiePanel 临时背包区新增「⚗️ 临时制作台」面板，`canCraftInSortie`（读 runRef.current，事件内调用）/ `doCraftInSortie`（消耗 carriedLoot 对应 loot、产出对应药品 loot 入本局背包）。UI 用 `carried` 快照算持有量、禁用读 ref 的渲染期调用（守 react-hooks/refs）；`isOver` 时禁用合成。
  - ③ 废土币显示：`content.ts` `credits` 由 `value:25,qty:1` 改为 `value:1,qty:25`，临时背包显示变为「废土币 ×25 = 25 废土币价值」；`bankLoot` 仍按 `value*qty=25` 折算，玩法数值不变。
- 版本：package.json 1.0.3→1.0.4；README 顶部版本号与新增「v1.0.4（本地待发）」更新日志。仍**未 push**，待用户本地验收后开 VPN 推 1.0.4。
- 验证：tsc 0 错、lint 仅余 hono.ts×2（按约定不动，含新增 smoke_req3.ts 也加 eslint-disable）、build:client 过（3.53s）、新增 `scripts/smoke_req3.ts` 14 断言全过（fight 后 xpGained 增长、夹板清骨折、splint 配方与 LOOT/映射、废土币 value1/qty25 折算 25、grantSortieXp 升级仍清伤势）。
- 坑（复现）：本次多处在 state.ts / content.ts 的 Edit 首次调用返回「Successfully edited」但实际未落盘（与上一轮一样的 flakiness），需 re-read 复核关键行（MedicineId 联合类型、MedCraftRecipe 接口、LOOT.credits、LOOT_MEDICINE_MAP）确认后再收尾，否则 tsc 会报 splint 不可赋值 / sortieNeeds 不存在。

## v1.0.4 补充：装备库排序+勾选修复 + 撤离结算折算修复（用户最新反馈，未 push）
- 用户要求：与上次 3 项 bug 修复（装备库筛选/废土币改版/日志整合）一起整合为 **1.0.4 的补充**，本地**不进行版本更迭**（仍保持 1.0.4 本地待发），没推 GitHub 前都只是下一版本整合。已把原先误升到 1.0.5 的 package.json 退回 1.0.4、README 顶部退回 v1.0.3、原 v1.0.5 更新日志并入 v1.0.4（本地待发），并把本轮 2 项修复也写入该条目。
- 新反馈①（背包装备库）：装备应从最高阶开始降序显示；批量卖出勾选异常——点一件有时卡住不能取消、有时连带勾选属性完全相同的其他装备、有时复制/假显示。
  - 根因：React 列表 `key` 用 `g.id`，当同一装备 id 在 `state.gear` 出现两次（bankLoot 把同一 loot.gear 推两次）时 key 碰撞 → 勾选串号/假显示/卡顿。
  - 修复：`ownedGear` 改为按 `tier` 降序（同级按 value 降序）；列表 `key` 改为 `${g.id}-${gi}`（碰撞免疫）；`state.ts bankLoot` 推装备前 `if (!gear.some(x=>x.id===droppedGear.id)) gear.push(droppedGear)` 防重复入库。材料 `quantity` 漏写改为 `qty`。
- 新反馈②（撤离结算）：废土币撤离后 1:1 直接折算入金额；其余物资仅估值、入库为材料/装备/药品，需贩卖/回收才折算为金额。原 bug：`bankLoot` 把材料 `value*qty` 一并累加进 `coins`，导致估值 115（废土币25+材料90）撤离后金额暴涨大几十。
  - 修复：`bankLoot` 删除 `coins += item.value*qty`（材料仅入库、不再折算）；`persistRunResult` 拾荒嗅觉金币加成由「基于物资估值」改为「基于本局搜刮废土币 `carriedCredits`」（与只折算废土币规则一致）；撤离结算文案改为区分「废土币×N 1:1 折算」与「带回物资估值⛁M（入库，需贩卖/回收才折算）」；`carriedValue` 不再含 carriedCredits（废土币单独行显示）。
  - 货币来源核对：撤离金额 = `carriedCredits`（废土币 1:1）+（若有）拾荒嗅觉基于废土币的加成；装备回收(963/1213) 与 废土市场「回收材料」(views.tsx:677) 是合法「贩卖」途径。
- 验证：tsc 0 错、build:client 过（3.54s）。仍**未 push**，待用户本地验收（http://localhost:5173，开 VPN 后才可发 1.0.4）。

## v1.0.4 最终补全：金币获取加成直抵废土币 + 发布 GitHub
- 用户新需求：金币获取加成（拾荒嗅觉/装备词条）应直接体现在「搜刮到的废土币数量」上——无加成角色每轮搜得 25 废土币，100% 加成角色搜得 50。
  - 实现：`ExtractionEngine.grantLoot` 发放货币时 `total = round(base * (1 + runCombatBonus(state).coinBonus))`，`base=(value*qty)=25`，bonus 由 `runCombatBonus`（bonusBase + 当前穿戴装备 combat.coinBonus）实时计算（含副本内换装）；`state.carriedCreditsBonus` 累计加成部分仅用于撤离文案提示，已含在 carriedCredits 内，persistRunResult 不再单独加算（避免重复）。`lootCorpse` 经 grantLoot 也受益。
  - 同步移除 persistRunResult 内 `special=aggregateGearCombat(...)` 与 `coinBonusCoins` 单独加算逻辑（现已 baked-in），并清理 route.tsx 中 `aggregateGearCombat` 未用导入。
- 发布（2026-09-07 晚）：整合本轮 + 此前全部 1.0.4 修复，提交 `21fdcdf`（9 文件 +334/-96），`git tag -a v1.0.4`，`git push origin main --tags` 成功（f8b64dd..21fdcdf，VPN/7890 代理开启一次成功）；随后补一个 docs 提交 `b27b4a4` 校正 README 顶部版本号与更新日志标题（v1.0.4（2026-09-07））。tag 钉在 21fdcdf 功能提交，docs 提交在其上。
- 验证：tsc 0 错、build:client 过（3.57s）。
