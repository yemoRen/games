# 2026-09-07 工作日志

## v1.0.5 收尾 + 推 GitHub
- 在已落地的 7 项 bug 修复基础上，补 2 项改动并作为 v1.0.5 推送：
  1. 出击系统消息日志自动滚动到最新（SortiePanel 内 logScrollRef + useEffect，依赖 run.log 末条；plog 为 push 故滚到底部）。
  2. 撤离交互重做：「前往撤离点」→「撤离」并仅在 atExtract 高亮、否则灰色；移除「抄近路直奔最近撤离点」按钮；本局撤离点名带危险等级（如 医院侧楼 危4、深井竖坑 危5）；清理 goToExtract/doGoExtract 死代码。
- 验证：tsc -b tsconfig.app.json 0 错；vite build 0 错。
- README 顶部版本 + 版本更新日志新增 v1.0.5 条目；package.json 已为 1.0.5。
- git：提交全部 v1.0.5 改动（排除 scripts/smoke_req3.ts）、tag v1.0.5、push origin main --tags（需用户本机 Clash 代理 7890 开启）。
- ✅ 已推送：commit 39f2ba2，git tag -a v1.0.5，git push origin main --tags 成功（PUSH_EXIT=0）。GitHub 远程已含 v1.0.5 标签。

## v1.0.6 五项改动（本地完成·待指令推送）
用户要求把以下 5 项作为 v1.0.6 攒着，等指令再发；本次仅本地改完并跑构建验证，未推 GitHub、未打 tag、未改 package.json 版本号。

1. **Bug1 取消「向撤离点突围放弃搜刮」选项**
   - `src/react-app/routes/survival/play/route.tsx`：移除遇敌 `🚁 向撤离点突围（放弃搜刮）` 按钮（`doEncounter('extract')`）。
   - `src/shared/engine/extraction/ExtractionEngine.ts`：删除 `resolveEncounter` 的 `case 'extract':`（曾令非撤离点直接撤离），并删注释 `突围奔赴撤离点`。
   - `src/shared/engine/extraction/types.ts`：`EncounterAction` 联合改为 `'fight' | 'sneak' | 'throw'`（去掉 `'extract'`）。

2. **Bug2 战力统计最终六维值**
   - `src/shared/engine/survival/state.ts`：新增 `effectiveAttributes(state, survivorId)`（基础属性 + 已装备六维 modifiers 加成）。
   - `recomputeMaxHpIncludingGear` 内 `power = computePower(eff)`；`allocateFreePoint`/`chooseTraitPick` 的战力重算也改用 `computePower(effectiveAttributes(...))`，不再用基础 `p.attributes`。

3. **Bug3 装备体质→血量同步**
   - 同一 `recomputeMaxHpIncludingGear`：改用 `deriveMaxHp(eff, totalHpBonus)`（eff 含装备体质），穿脱装备经 `equipGear`/`unequipGear` 自动同步最大/当前血量。

4. **Bug4 词条单行彩色显示**
   - `route.tsx` 装备库卡片：灰色 `g.affixes` 行 + 彩色 chips 行合并为一行彩色（`text-emerald-300`，含 affixes 与 modifiers 徽标）。

5. **Bug5 错误页改搜打撤相关**
   - `src/react-app/App.tsx` 的 `RootRouteErrorView`：标题 `道途异常`→`废土异常`；kicker `{APP_TITLE}`→`全民求生・系统搜打撤`；`道途出现偏差`→`废土航向偏移`；返回首页 `href="/"`→`/survival`（2 处）、`href="/game"`→`/survival/play`。

- 验证：`bun run build:client`（tsc -b tsconfig.app.json + vite build --mode client）BUILD_EXIT=0，零类型/构建错误。
- **注意**：本会话发现 Edit/Read 工具存在沙箱视图与真实磁盘不一致，App.tsx 的 Bug5 上一轮 Python 脚本因空格锚点不匹配在 save 前抛错、整文件未落盘，已用正确 16 空格锚点重做并复核真实磁盘。
- 待用户指令：bump package.json 至 1.0.6 → commit → `git tag -a v1.0.6` → `git push origin main --tags`（需本机 Clash 代理 7890 开启）。

## 6 项本地改动（续 v1.0.6，本地完成·未推送）

接 v1.0.6 后用户追加 6 项本地开发需求，全部落地并 `bun run build:client` 验证通过（BUILD_EXIT=0，仅 chunk>500kB warning）。同样沿用「Bash+Python 落真实磁盘」规避沙箱视图偏差。

1. **#129 首屏加载页仍显示万界道友** — `AppBootScreen.tsx` 的 `isWastelandRoute()` 增加匹配 `/`：`return p === '/' || p.indexOf('/survival') === 0;`。首屏 `/`（经 indexRedirectLoader 重定向前）也走搜打撤加载页。
2. **#130 背包装备栏增加已穿戴属性增益统计** — `play/route.tsx` 装备栏「主槽装备常驻生效」段后插入统计，调用 `gearAttrBonus(state, active.id)`，渲染非零项彩色徽标。
3. **#131 临时背包装备词条与背包统一单行彩色** — `GearBonusChips` 增加具名词条展示：`affixText = (gear.affixes ?? []).join('、')`，与彩色 chips 同行；无加成显示「无属性加成」。装备栏/装备库/临时背包复用同一组件。
4. **#132 撤销驻防「出击初始HP」+ 出击页显示驻防全属性加成** — `economy.ts` medbay `hpPerLevel: 12 → 0`；`state.ts` `buildSortieLoadout` 的 hpBonus 去掉 `bonuses.startHpBonus`；删除出击页驻防面板 `出击初始HP +{...}` 整行；在出击页顶部（`<section>` 内、`{!run && (` 之前）插入驻防全属性展示 IIFE（合并 attrBonus+factionAttrBonus，逐项彩色徽标，无加成返回 null）。
5. **#133 出击进图驻防体质血量同时加当前/最大** — `start()` 改为 `garrisonAttrHp = (vitality*20 + endurance*3)`（含势力声望），`startMax = baseMax + garrisonAttrHp`（最大血量）、`startHp = min(startHp + garrisonAttrHp, startMax)`（当前血量），`createRun` 用 `startMaxHp: startMax, baseMaxHp: baseMax`。修复切换角色出击不吃驻防全属性血量增益的根因（`status.maxHp` 由 recomputeMaxHpIncludingGear 计算、不含避难所/势力 attrBonus）。
6. **#134 训练场削弱为每级 1 点全队属性** — `economy.ts` gym `attrPerLevel: { vitality:1, strength:1 } → { vitality:1 }`。

- 附带清理：`demo.ts` 移除 `出击初始HP: bonuses.startHpBonus` 调试输出（startHpBonus 现已恒为 0）。`state.ts` 已无 startHpBonus 引用；play 路由 UI 无残留 `出击初始HP`。
- 验证：`bun run build:client` 通过（tsc -b 0 错 + vite 构建 0 错）；dev server http://localhost:5173/ 仍在后台运行（HMR 已套用）。
- **未推送**：v1.0.6 与本次 6 项均仅本地。发版需用户指令：bump package.json → commit → `git tag -a vX.Y.Z` → `git push origin main --tags`（本机 Clash 代理 7890 开启）。
