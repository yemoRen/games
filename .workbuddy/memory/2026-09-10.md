# 2026-09-10 工作日志

## v1.0.13 累积（第⑤项落地）：命中/闪避战斗结算重做
- 根因：原 `DamageSystem` 用 `dodge = clamp(闪避率 − 命中率, 3, 45)`，命中/闪避同源 SPEED 同形曲线、命中 base/渐近恒高于闪避 → net dodge 恒为负 → 永远 3% 地板，闪避形同虚设。
- 定稿（用户指令「独立 / 参数不动 / 闪避基础10 / 都软曲线」）：
  - `AttributeSet`：命中率 = 0.70 + curve(max(0,SPEED-10), 11, 0.28)（软曲线渐近 0.98）；闪避率 = 0.10 + curve(max(0,SPEED-10), 25, 0.50)（软曲线渐近 0.60）。
  - `DamageSystem`：独立结算 有效命中 = max(0.20, 命中率×(1−闪避率))，单次掷骰定命中，未命中按比例拆「闪避/落空」；移除原 `clamp(闪避−命中,3,45)`。
- 同步更新 `AttributeSet.test.ts`（4 处断言）、`ActionExecutionSystemIntegration.test.ts`（2 处 dodge 断言 → 地板/快守闪慢攻）。
- 验证：battle-v5 全量 370 测试通过；`bun run build:client` exit 0。
- 状态：已落盘、未 commit/未 push；v1.0.13 现累积 ①②③④⑤ 共 5 项，等「发包」指令。
- 坑：Edit 对 `AttributeSet.test.ts` 两处替换曾报成功但未落盘，用磁盘一致文本重替才生效（沙箱视图不一致，已 Python 复核）。

## 发包 v1.0.13（2026-09-10 完成）
- commit `c124047`，tag `v1.0.13` 指向该提交；已 `git push origin main`（快进 775cfe6..c124047）+ 推标签到 GitHub（games 仓库）。
- 五项：①出击中禁遣散 ②潜行绕行下调 ③力量注释 ④伤势注释 ⑤命中/闪避结算重做（软曲线+独立结算+命中地板）。
- ⚠️ 坑：非 GUI 环境 `git push` 被 Git Credential Manager 弹窗挂起超时；解决=设 `GCM_INTERACTIVE=never GIT_TERMINAL_PROMPT=0` 读 Win 凭据缓存 token（已缓存，`credential-manager get` 退出码 0）。
- 按约定 `.workbuddy/` 记忆未随本次推送（仅源码+文档 9 文件入 commit）。
