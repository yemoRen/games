# 2026-09-09 工作日志

## v1.0.11 第三轮补充 + 正式发布（commit `5bf3557` · tag `v1.0.11` · 已 push origin/main）
用户指令：「继续补充 v1.0.11 攒着 + 2 项，修改好以后直接发包，做好更新日志管理。」

### 第三轮 2 项改动
1. **角色属性 UI 合并进经验条卡片、与经验条共处同一固定区域**：把出击内角色属性 / 增益 / 伤势区块从独立的 `sticky top-[152px]` 次级块移入经验条卡片（经验进度条之后、`自由属性点`之前），随经验条一同固定滑动；删除「角色属性（红字=伤势削减）」文字标签。落点 `play/route.tsx`。
2. **濒死成员禁止遣散**：`state.ts` 的 `dismissSurvivor` 引擎层加濒死守卫（`survivorStatus[survivorId].dyingUntil` 未过期则拒绝并写日志 `【遣散】X 正处于濒死状态，无法遣散，请先救治。`）；`play/route.tsx` 战团成员卡片的遣散按钮在濒死时显示灰态「濒死·不可遣散」。双保险防带伤成员被悄悄移除。

### 发布动作
- `package.json` 1.0.10 → 1.0.11；README 顶部版本号 + 新增 v1.0.11 更新日志段（整合三轮共 11 项）。
- 提交 `5bf3557`，`git tag -a v1.0.11`，`git push origin main --tags` 成功（Clash 7890 代理，关沙箱推送）。
- 仅提交源文件 + README + package.json + 项目记忆；`.workbuddy/scratch/` 与 `scripts/`(无关产物) 保持未跟踪、未提交。
- `bun run build:client` 通过（✓ built in 3.50s）。

### 关键设计结论（已固化进 MEMORY.md）
- **战斗引擎「数值悬崖」**：敌人基础六维放大 ~5% 即让通关胜率从惨胜跌到 0%，难度曲线极陡不可微调；副本难度靠「等级门槛 + 区域危险度(爆率) + 深度词缀(遇怪)」三层间接控制，`ENEMY_DANGER_SCALE` 钩子当前全 1.0 留待实测。
- **内容「共享池」陷阱**：7 张图曾共用全局 ZONE_POOL 致 16 区名雷同；现 `DangerZone.subZones` 各图独立，fallback 用全局。
