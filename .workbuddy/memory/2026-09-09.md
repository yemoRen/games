# 2026-09-09 工作日志

## v1.0.11 第三轮补充 + 正式发布（commit `5bf3557` · tag `v1.0.11` · 已 push origin/main）
用户指令：「继续补充 v1.0.11 攒着 + 2 项，修改好以后直接发包，做好更新日志管理。」

### 第三轮 2 项改动
1. **角色属性 UI 合并进经验条卡片、与经验条共处同一固定区域**：把出击内角色属性 / 增益 / 伤势区块从独立的 `sticky top-[152px]` 次级块移入经验条卡片（经验进度条之后、`自由属性点`之前），随经验条一同固定滑动；删除「角色属性（红字=伤势削减）」文字标签。落点 `play/route.tsx`。
2. **濒死成员禁止遣散**：`state.ts` 的 `dismissSurvivor` 引擎层加濒死守卫（`survivorStatus[survivorId].dyingUntil` 未过期则拒绝并写日志 `【遣散】X 正处于濒死状态，无法遣散，请先救治。`）；`play/route.tsx` 战团成员卡片的遣散按钮在濒死时显示灰态「濒死·不可遣散」。双保险防带伤成员被悄悄移除。

### 发布动作
- `package.json` 1.0.10 → 1.0.11；README 顶部版本号 + 新增 v1.0.11 更新日志段（整合三轮共 11 项）。
- 提交 `5bf3557`，`git tag -a v1.0.11`，`git push origin main --tags` 成功（Clash 7890 代理，关沙箱推送）。
- 仅提交源文件 + README + package.json + 项目记忆；`.workbuddy/scratch/` 与 `scripts/`(无关产物) 保持未跟踪、未提交。
- `bun run build:client` 通过（✓ built in 3.50s）。

### 关键设计结论（已固化进 MEMORY.md）
- **战斗引擎「数值悬崖」**：敌人基础六维放大 ~5% 即让通关胜率从惨胜跌到 0%，难度曲线极陡不可微调；副本难度靠「等级门槛 + 区域危险度(爆率) + 深度词缀(遇怪)」三层间接控制，`ENEMY_DANGER_SCALE` 钩子当前全 1.0 留待实测。
- **内容「共享池」陷阱**：7 张图曾共用全局 ZONE_POOL 致 16 区名雷同；现 `DangerZone.subZones` 各图独立，fallback 用全局。


## v1.0.12 本地累积（已发包 2026-09-09，tag v1.0.12）
用户指令：「先本地修改 v1.0.12 攒一批」（明确攒着、暂不发包）。

1. **霸主唯一性：取消 boss 参与转移伏击**（核心修复）。
   - 根因：`branchZone`/`generateZoneGraph` 让霸主区敌池含 `bossEnemy`（`boss:true`）；`moveToNode`(1649) 与 `advanceBranch`(1686) 的「转移伏击」直接 `pickEnemy(state, rng)` 用 `state.zone.enemies`，一旦身处/进入霸主区就会被抽出 boss → 可反复转移刷 boss。
   - 修复：两处伏击改为 `ambushPool = state.zone.enemies.filter(e => !e.boss)`，池为空则跳过伏击。霸主仍仅由 `rollEncounter` 在「霸主区第三次搜刮」登场（991-993 行，未动），保持唯一性。
   - 落点：`ExtractionEngine.ts`（moveToNode + advanceBranch 各 1 处）。
2. **场景 + 系统消息日志 整合为更紧凑单一面板**。
   - 场景 `pre` 加 `line-clamp-3`（限高 3 行，全文仍存于 log），日志 `max-h-[150px]→[112px]`、字号 10→9px，内边距 `p-3→px-2.5 py-1.5`，两块标签压缩为 9px 小字。
   - 落点：`play/route.tsx`（场景/日志面板，约 2535 行）。
- `bun run build:client` 通过（✓ built in 4.37s）。未 bump 版本、未 commit、未 push。


## v1.0.12 第三轮（本地累积，未发包）
用户原话：「继续做 v1.0.12 的修改，攒着。1、将「携带估值 0 废土币」放在对局剩余时间那一行的最右侧。2、将等级经验值 UI 修改在生命值下方，和生命值 UI 差不多大小。3、其余你自己排版一下，做到合理且节省 UI 空间。」（用户特别强调面向手机端，所以要紧凑）

**出击页头部 sticky 面板重构**（`play/route.tsx` 一整段替换）：

- **Row 1**（`flex items-center justify-between`）：`⏱ 对局剩余 X:XX / 40:00`（左） + `💰 携带估值 N 废土币`（右）。两件事一行，时间与货币一目了然。
- **Row 2**（`flex flex-wrap`）：📍 位置·危·深度·搜刮 + 🚁 撤离点 + 🔥 威胁 + 🛡 安全箱 —— 4 个 meta 紧凑成行。
- **Row 3**（`grid grid-cols-2 gap-3`）：`❤ 生命 [bar]` | `⬆ Lv.X 经验 N/M [bar]` —— 经验条与生命条同尺寸（`h-2`），右侧带「自由点 ×N」小徽标。**核心要求达成**。
- **Row 4**（`grid grid-cols-3 gap-3`）：🛡 护甲 | 🔫 弹药 | 🎒 负重 —— 3 列纯文本（无 bar），省高度。
- 6 维属性 / 增益 buff / 自由属性点 / 词条三选一 **全部并入主面板**，**删除原独立 sky-950 蓝框卡片**（`rounded-lg border border-sky-900/50 bg-sky-950/20 p-3`），仅保留 `border-t` 细分隔线，省一个边框+内边距。
- 主面板 `p-4 → p-3`、内边距统一收紧（`mt-3 → mt-2.5`、`mt-2 → mt-1.5`），整体高度比之前矮约 1/3，更适配手机视口。

`bun run build:client` 通过（✓ built in 3.59s）。Vite HMR 已推送变更到 localhost:5173/。未 bump 版本、未 commit、未 push。

## v1.0.12 小修（本地累积，未发包）
用户追加：「把 🛡 安全箱 也放在 Row 4，和护甲/弹药/负重 在一起」。
- `play/route.tsx`：从 Row 2（位置/撤离/威胁/安全箱 单行）删除安全箱；Row 4 由 `grid-cols-3` 改 `grid-cols-4`，新增第 4 列「📦 安全箱 secureUsed/SECURE_BOX_SLOTS」。Row 2 现仅剩位置/撤离点/威胁三项。
- 仍属 v1.0.12 本地累积，未 bump、未 commit、未 push。

## v1.0.12 第四轮（本地累积，未发包）
用户：「1、整合 💊使用药物恢复 🧪增益补给 成1个ui区域，左右排版。2、区域图当前区域外框颜色随深度1~7从白变红（同装备阶级颜色）。」
- **① 药物+增益合并**：`play/route.tsx` 原两个独立卡片（💊使用药物恢复 / 🧪增益补给）合并为同一 `rounded-lg border border-zinc-800 bg-zinc-900 p-3` 卡片，内部 `grid grid-cols-1 gap-3 sm:grid-cols-2`（手机纵向、宽屏左右双栏），各自内容原样保留；外层条件 `availableMeds.length>0 || (buffSpec && buffStock>0)` 任一存在即显示。
- **② 当前区外框深度配色**：当前所在区域 `<div>` 由固定 `border-emerald-500/60 bg-emerald-500/5` 改为 `style={{ borderColor: tierColor(depth-1)+'99', backgroundColor: tierColor(depth-1)+'14' }}`，区域名文字也用 `tierColor(depth-1)`。depth1→白(#cbd5e1)、depth7→红(#f87171)，与装备七阶阶级色完全一致（复用已导入的 `tierColor`）。
- `bun run build:client` 通过（✓ built in 3.65s）。已随 v1.0.12 发布（commit 见 git log，tag v1.0.12）。

## 发布记录 v1.0.12（2026-09-09 已发包）
- bump package.json 1.0.11→1.0.12；README 顶版本号 + 新增 v1.0.12 更新日志章节；build:client 通过（3.49s）。
- 提交源文件 src/ + README.md + 项目记忆，排除 .workbuddy/scratch、scripts/；git tag -a v1.0.12 → git push origin main --tags（沙箱禁用，需 dangerouslyDisableSandbox）。
## v1.0.12 补充（本地累积，未发包；4 项修复）
用户原话：「作为 v1.0.12 的补充，修改一下 ui：1、手机端护甲/弹药/负重/安全箱排版错开没对齐 2、出击中升级时在出击页面加体质有用，但切换到角色页面加体质最大血量未增加 3、若一次性多次升级自由属性点数量没错但天赋三选一只有一次选择 4、出击过程中升级时把「自由点 ×N」挪到「分配自由属性点」那里合并改为「分配自由属性点 ×N」」。

修复落地：
1. **Row 4 排版（手机端防折行）**：`play/route.tsx` 把「护甲/弹药/负重/安全箱」4 列从 `flex items-end justify-between`（横排 label+value 撑爆窄列）改为 `flex flex-col items-start`（label 在上 value 在下，纵向 stack）。
2. **角色页体质加点 → maxHp 同步修复**：`state.ts:recomputeMaxHpFor` 去掉 `if (newMax <= st.maxHp) return state` 早返守卫，改为始终按 derived 写回（与 `recomputeMaxHpIncludingGear` 同口径）。修复旧档若 `st.maxHp` 因历史 bug/旧版本被错误抬高时，加点因「newMax < st.maxHp」被早返吞的边缘 case。引擎层冒烟测试：体质 20→21 → maxHp 845→865（+20）、连续 2 次 → 885。`scripts/smoke_req.ts` 测试夹具补 `equipped/gear/facilities/factionRep` 字段。
3. **连升 N 级 → 词条三选一生成 N 组**：`grantSortieXp` 把「无候选才生成」改为「每升 1 级 push 3 候选」累积式；`chooseTraitPick` 签名改为 `(state, survivorId, pickSetIndex, candidateIndex)` 双下标，选中后整组 3 个一次性从待选列表移除。`play/route.tsx` 角色页 + 出击页均按 3 个一组 chunk 渲染多个「三选一」区块，多组时标题显示 `(1/3, 三选一)` 进度。引擎层冒烟：1000 XP → 连升 5 级 → 15 自由点 + 15 候选 = 5 组三选一。
4. **「自由点 ×N」徽标合并到分配标签**：`play/route.tsx` 两处（出击页 Row 3 / 角色页等级行）删除独立的 `自由点 ×{n}` 徽标，改在「分配自由属性点」标签里写 `分配自由属性点 ×{n}（每点 +1）`，避免重复。

回归：build:client 通过（3.49s）；smoke_req / smoke_req3 / v1.0.12 补充 smoke 全部通过。

✅ 已推送（2026-09-09 11:59）：commit `bac7bb0`；`git tag -f v1.0.12` → 新对象 `bfd5358`（解引用至 `bac7bb0`）；强推标签 `git push -f origin refs/tags/v1.0.12`；并普通快进 `git push origin main`（2604cb3→bac7bb0，非 force）。远端 main 与 tag 均指向 `bac7bb0`，版本号仍 1.0.12。README 增补 v1.0.12 补充修复小节。

## v1.0.12 补充第 5 项（本地累积，未发包 · 倒扣回归修复）
用户原话：「我派了个1级新成员去升级，副本中升级，我在角色页面加体质，出现了血量倒扣...分析研究下原因，修改，还是作为1.0.12版本的补充，等我指令发包」+ 钟泽山（初始6体质，带基地buff进副本，升级，切角色页加体质，倒扣）。

- **根因**：v1.0.12 补充（commit `bac7bb0`）为修「角色页加点不加血」误删 `recomputeMaxHpFor` 的早返守卫 `if (newMax <= st.maxHp) return state`，改为无条件按 derived 写回。新人 `freshStatus` 固化 600 新手保护缓冲（6 体质 derived≈538 但 st.maxHp=600），加 1 体质 derived≈558<600 → st.maxHp 被压到 558；`syncProfileMaxHpDeltaToRun` 仅 delta>0 才同步当前血，导致副本 hp.max↓558 而 hp.current 仍 600 → UI「当前>上限」怪象（用户截图 623/581 ≈ 42 差额吻合）。
- **修复**：恢复早返守卫（仅 derived 严格 >st.maxHp 才提升）；清理函数上方误导性注释（"v1.0.12 补充：去掉早返"）+ 函数体内过时内联注释（"之前漏算装备...吞掉加点增量"）。
- **连带修正**：README v1.0.12 补充第 2 项改为「上一版误改」说明，并新增第 5 项修复条目；MEMORY.md 原「⚠️ recomputeMaxHpFor 不要早返」错误铁律反转为「必须保留早返」（这条铁律正是上次误删的源头，已纠正以防复发）。
- **关于 issue ② 角色页加点不加血**：实为新人 600 缓冲吸收早期体质点（by-design 新手保护，非扣血 bug）；普通角色加点守卫不触发、正常生效。若要新人加点立即超过 600 需把缓冲改设计为叠加式（属设计决策，本次未改，待用户定夺）。
- 构建：`bun run build:client` ✅ 通过（tsc -b 零类型错误，vite `✓ built in 8.09s`；注意首次因 WorkBuddy 安全删除垫片拦截 dist/assets 批量清空而失败，清空 dist 后重build 通过）。未 bump 版本、未 commit、未 push，等用户「发包」指令后走 commit + `git tag -f v1.0.12` + 强推标签 + 普通快进 main。

## v1.0.12 第5项 已发包（2026-09-09 12:58）
- commit `775cfe6` → `git tag -f v1.0.12`（bfd5358→775cfe6）→ `git push origin main`（bac7bb0..775cfe6 快进）→ 强推标签（forced update）。
- 远端核验：origin/main 与 origin/v1.0.12 均指向 775cfe66ae478f07de4a6d6e44525ddc9147fb33，与本地 HEAD 一致。发包完成。

## v1.0.13 第1项（本地累积，未发包 · 出击中成员禁止遣散）
用户原话：「战团成员在出击中应不能被遣散，我刚刚尝试了出击过程中遣散出击的成员能被遣散，这不合理。」
- 根因：战团成员卡片底部遣散分支只拦截 `isDying`（濒死），没拦 `inSortie`（正在副本中）。`inSortie = sortieId === s.id` 在同作用域已定义（play/route.tsx:729）但未用于遣散判断。
- 修复：在 play/route.tsx 战团列表遣散分支最前加 `inSortie ?` 优先拦截，显示「出击中·不可遣散」灰态并禁用，与「濒死·不可遣散」并列。
- 关键约束：`run`（副本运行态）是 React 组件层 `useState/useRef`，**不进 `SurvivalGameState` 存档**，故引擎 `dismissSurvivor(state, id)` 读不到「谁在副本中」，拦截只能在 UI 层做（勿在引擎层加不可靠守卫）。
- 配套：package.json version 1.0.12→1.0.13；README 新增 v1.0.13 累积段（顶部）。
- 构建：`bun run build:client` ✅ 通过（tsc 零错误，vite `✓ built in 8.12s`；dist 清空因 WorkBuddy 安全删除垫片拦截，清空后重 build 通过）。
- 未 commit / 未 push / 标签仍为 v1.0.12；等用户「发包」指令后走 commit + `git tag -f v1.0.13` + 强推标签 + 普通快进 main。

## v1.0.13 第2项（本地累积，未发包 · 潜行绕行成功率下调）
用户原话：「敏捷10（基准），普通敌绕行成功率改为30%，Boss 成功率改为15%」→「作为v1.0.13的修改攒着」。
- 改动：`src/shared/engine/extraction/ExtractionEngine.ts` 的 `resolveEncounter` 潜行分支基准值 `(boss ? 0.35 : 0.72)` → `(boss ? 0.15 : 0.30)`；钳制 `Math.max(0.1, Math.min(0.95, ...))` 不变；敏捷加成 `sneakBonus=(speed-10)*0.03` 不变。
- 效果（敏捷10基准）：普通敌 30% / Boss 15%；低中敏捷段绕行容错被压低，Boss 几乎遇必战，逼用「投掷物脱离」（烟雾弹/闪光弹，必脱离、不靠敏捷）。属难度上调。
- 配套：README v1.0.13 段追加第2项说明。构建 `bun run build:client` ✅ 通过（BUILD_EXIT=0，vite `✓ built in 8.27s`；dist 清空因安全删除垫片拦截，单独 rmtree 授权后重建通过）。
- 仍累积中：未 commit / 未 push / 标签仍 v1.0.12。等用户「发包」指令后走 commit + `git tag -f v1.0.13` + 强推标签 + 普通快进 main。

## v1.0.13 第3项（本地累积，未发包 · 力量背包注释修正）
用户原话：「顺手修掉吧（力量注释每5点→每1点），然后告诉我意志属性的内容」。
- 改动：`src/shared/engine/extraction/ExtractionEngine.ts` 的 `deriveAttrEffects` 文档注释「力量 → 背包容量（每 5 点 +1 格）」→「白字每 1 点 = 1 格」（L140），与代码（runPackCapacity，白字每1点=1格）对齐。纯注释，无逻辑变化。
- 配套：README v1.0.13 段追加第3项。未单独 bump 版本（已是 1.0.13）。
- 查询结论·意志(willpower)：①副本层派生 `shockResist = max(0.55, 1 - willpower*0.015)`（震伤抗性系数）但**全工程无消费点=死派生**；②实际生效在 survival 层 `recovery.ts: rollCombatInjuries` 的 `willResist` 同源公式，削减 `shellShock`(震伤) 概率 `shockP=(0.12+0.38*(1-p))*willResist`；③伤势扣分：意志被 shellShock(感知/意志-1/3)、infection(体质/意志-1/4)、fatigue(全六维-1/4) 扣；④增益药剂+2意志；⑤battle-v5 经 WILLPOWER 属性「更难被敌方节奏带偏」。数值：意志10→×1.0，意志20→×0.70（震伤概率乘子），下限0.55。
- 待用户定夺：副本层 `shockResist` 死派生——要么在副本 combat 接上 `shockResist`，要么删掉无用派生（属整洁性，未改）。
- 仍累积中：未 commit / 未 push / 标签仍 v1.0.12。等用户「发包」指令后走 commit + `git tag -f v1.0.13` + 强推标签 + 普通快进 main。

## v1.0.13 第4项（本地累积，未发包 · 伤势抗性注释修正）
用户原话：「顺手修掉（recovery.ts 意志注释×1.0→0.85），然后告诉我耐力的内容」。
- 改动：`src/shared/engine/survival/recovery.ts` 的 `rollCombatInjuries` 两处抗性注释：意志「10→×1.0」→「10→×0.85 / 20→×0.70 / 30→×0.55封底」；体质「10→×1.0」→「10→×0.90 / 25→×0.75 / 40→×0.60封底」（同款注释错，实际代码 `1 - willpower*0.015` 与 `1 - vitality*0.01`）。纯注释，逻辑不变。
- 配套：README v1.0.13 段追加第4项。未单独 bump 版本（已是 1.0.13）。
- 查询结论·耐力(endurance)：①副本续航 `staminaMinutes = endurance * 1.5` 分钟（v1.0.8 由 ×2 改 ×1.5），超时后 `checkFatigue` 每次时间推进判定疲惫 p=min(0.6, 0.05×超时分钟数)，疲惫=全六维−1/4 debuff（兴奋剂/营养剂消除）；②气血上限 +3/点（deriveMaxHp 与 equippedHpBonus）；③伤势扣分：fracture(力量/敏捷/耐力 −1/3)、bleeding(体质/耐力 −1/3)、fatigue(全六维 −1/4)；④battle-v5 经 ENDURANCE「续航更持久，消耗战有利」；⑤天赋/词条挂钩：铁甲(endurance+3)、钢骨(endurance+5+hpBonus20)、护甲槽(endurance3+体质1)、腿甲(speed3+endurance1)、'皮糙肉厚'(endurance4+体质2+hpBonus25) 等；⑥战力评分 耐力×1.1。
- 仍累积中：未 commit / 未 push / 标签仍 v1.0.12。等用户「发包」指令后走 commit + `git tag -f v1.0.13` + 强推标签 + 普通快进 main。
